name: admin_data
description: Data layer for testnet admin panel
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  admin_domain:
    path: ../admin_domain
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  injectable: ^2.7.0
  get_it: ^8.0.3

dev_dependencies:
  build_runner: ^2.4.14
  injectable_generator: ^2.7.0
  test: ^1.25.8
  mocktail: ^1.0.4
