name: dashboard_domain
description: Dashboard feature domain layer
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  admin_domain:
    path: ../../../core/admin_domain
  get_it: ^8.0.3
  injectable: ^2.5.0

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  injectable_generator: ^2.7.0
