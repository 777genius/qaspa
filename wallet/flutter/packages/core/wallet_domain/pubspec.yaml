name: wallet_domain
description: Core domain layer with entities, value objects, and repository interfaces
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  decimal: ^3.2.1

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.2.3
  json_serializable: ^6.11.1
  test: ^1.25.8
