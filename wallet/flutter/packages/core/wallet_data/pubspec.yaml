name: wallet_data
description: Data layer with repository implementations and datasources
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  wallet_domain:
  wallet_rust_bridge:
  drift: ^2.23.1
  sqlite3_flutter_libs: ^0.5.28
  path_provider: ^2.1.5
  path: ^1.9.1
  synchronized: ^3.3.0+3
  logging: ^1.3.0
  modularity_core: ^0.1.0
  modularity_injectable: ^0.1.1
  injectable: ^2.7.0
  get_it: ^8.0.3

dev_dependencies:
  test: ^1.25.8
  mocktail: ^1.0.4
  drift_dev: ^2.23.1
  build_runner: ^2.4.15
  injectable_generator: ^2.6.2
