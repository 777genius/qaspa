name: wallet_rust_bridge
description: FFI/WASM bridge to kaspa-wallet-core Rust library
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  wallet_domain:
  flutter_rust_bridge: ^2.7.0
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.8
  mocktail: ^1.0.4
