name: send_domain
description: Send feature domain layer with use cases

publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  wallet_domain:
  injectable: ^2.7.0
  get_it: ^8.0.3

dev_dependencies:
  build_runner: ^2.4.15
  injectable_generator: ^2.6.2
  test: ^1.25.8
