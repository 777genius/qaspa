name: receive_domain
description: Receive feature domain layer with use cases.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  wallet_domain:
  injectable: ^2.7.0
  get_it: ^8.0.3

dev_dependencies:
  build_runner: ^2.4.15
  injectable_generator: ^2.6.2
