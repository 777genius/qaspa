name: onboarding_domain
description: Onboarding domain layer with use cases for wallet creation and import
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  wallet_domain:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  injectable: ^2.7.0
  get_it: ^8.0.3

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.2.3
  json_serializable: ^6.11.1
  injectable_generator: ^2.6.2
