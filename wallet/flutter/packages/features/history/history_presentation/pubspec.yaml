name: history_presentation
description: History feature presentation layer with UI and state management.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  modularity_flutter: ^0.1.0
  modularity_injectable: ^0.1.0
  flutter_mobx: ^2.2.1+1
  mobx: ^2.4.0
  go_router: ^15.1.2
  injectable: ^2.7.0
  get_it: ^8.0.3
  design_system:
  history_domain:
  wallet_domain:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  mobx_codegen: ^2.6.1
  injectable_generator: ^2.6.2
  modularity_test: ^0.1.0
  mocktail: ^1.0.4
