name: design_system
description: Shared Material 3 design system with green color scheme
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  wallet_domain:
  qr_flutter: ^4.1.0
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
