name: kaspa_wallet_mobile
description: Kaspa Wallet Mobile App
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  modularity_core: ^0.1.0
  modularity_flutter: ^0.1.0
  modularity_get_it: ^0.1.0
  modularity_injectable: ^0.1.0
  injectable: ^2.7.0
  get_it: ^8.0.3
  go_router: ^15.1.2
  onboarding_presentation:
  home_presentation:
  send_presentation:
  receive_presentation:
  history_presentation:
  settings_presentation:
  stealth_presentation:
  design_system:
  wallet_domain:
  wallet_data:

dev_dependencies:
  flutter_test:
    sdk: flutter
  modularity_test: ^0.1.0
  mocktail: ^1.0.4
  build_runner: ^2.4.15
  injectable_generator: ^2.6.2
