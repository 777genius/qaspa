#!/bin/sh
set -euo pipefail

if [ -d "${HOME}/.cargo/bin" ]; then
  PATH="${HOME}/.cargo/bin:${PATH}"
fi

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "clippy: kaspa-wallet-core (tests)..."
cargo clippy -p kaspa-wallet-core --tests

